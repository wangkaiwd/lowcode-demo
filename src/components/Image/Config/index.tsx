import { Form, Input } from 'antd';

const Config = () => {
  return (
    <>
      <Form.Item
        name={'src'}
        label={'图片url'}
      >
        <Input/>
      </Form.Item>
    </>
  );
};

export default Config;
